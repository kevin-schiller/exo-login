<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
</head>
<body>
    <form method="post" action="login.php">
        <label for="login"></label>
        <input type="text" name="login" id="login" placeholder="Enter your login" class="{% if errors.login is defined %}is-invalid{% endif %}" id="login" aria-describedby="loginHelp" value="{{ formData.login }}">
        {% if errors.login is defined %}
        <div class="invalid-feedback">
            {{ messages.login }}
        </div>
        {% endif %}

        <label for="password"></label>
        <input type="password" name="password" id="password" placeholder="Enter your password" class="{% if errors.password is defined %}is-invalid{% endif %}" id="password" value="{{ formData.password }}">
        {% if errors.password is defined %}
        <div class="invalid-feedback">
            {{ messages.password }}
        </div>
        {% endif %}
        
        <button type="submit" class="btn btn-primary">Envoyer</button>
    </form>
</body>
</html>